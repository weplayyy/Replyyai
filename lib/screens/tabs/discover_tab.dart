import 'package:flutter/material.dart';
import 'friends_tab.dart';

class DiscoverTab extends StatelessWidget {
  const DiscoverTab({super.key});
  @override
  Widget build(BuildContext context) => const FriendsTab();
}
